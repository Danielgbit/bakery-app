import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["plus.unsplash.com", "images.unsplash.com", "res.cloudinary.com", "cdn.shopify.com", "cdn.pixabay.com", "images.pexels.com"],
  },
};

export default nextConfig;
